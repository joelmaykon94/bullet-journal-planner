---
phase: 23
plan: 23.1
type: auto
autonomous: true
depends_on: ["22.1"]
---

# Plan 23.1: Ajustar o cálculo da posição da hora na linha do tempo

## Objective
Adjust the current time indicator red line positioning in the `TimelineTab` component to sit exactly on top of the current hour block and dynamically interpolate its top position within that block's actual height based on elapsed minutes, avoiding misalignments caused by hour blocks having varying heights.

## Tasks
- [ ] **Task 1: Add time tracking and measurement state to TimelineTab.tsx**
  - Import `useState`, `useEffect`, `useCallback`, and `useRef` hooks from `'react'`.
  - Declare a container `ref` for the timeline container.
  - Declare `now` state (updated periodically) and a `lineTop` pixel position state.
- [ ] **Task 2: Implement dynamic layout measurement in TimelineTab.tsx**
  - Add `data-hour={hourNum}` attribute to each hour `div` mapping.
  - Add a callback and effect that queries the container ref for `[data-hour="h"]` (where `h` is the current hour), retrieves its `offsetTop` and `offsetHeight`, and sets `lineTop = offsetTop + (minutes / 60) * offsetHeight`.
  - Ensure this function runs when `now` state changes, window resizes, or `items`/`timelineMobileView` change.
- [ ] **Task 3: Replace percent-based timeline line positioning with lineTop pixel value**
  - Replace the percentage-based absolute positioning in `TimelineTab.tsx` with the dynamically calculated pixel height `lineTop`.
- [ ] **Task 4: Verify task build compiles successfully**
  - Compile the application and ensure no type or compilation errors exist.

## Verification Criteria
1. The current time red line aligns precisely with the top of the current hour block when minutes are 0.
2. The red line moves down proportionally based on the minutes of the hour inside the current hour block, irrespective of whether the block contains tasks (meaning block heights may vary).
3. The project builds cleanly using `pnpm run build`.
