---
phase: 19
plan: 19.1
type: auto
autonomous: true
depends_on: ["18.1"]
---

# Plan 19.1: Removal of AI, Brain Dump, ADHD Copilot, Focus Guide, and Someday/Maybe Features

## Objective
Thoroughly clean up and remove AI integration (Local LLM workers, suggestions modals, decomp handlers), Brain Dump tab/modal, TDAH Copilot suggestions, AI in Habit Tracker, Focus Guide cards/modals, and the Someday/Maybe (GTD) tab/list. Ensure all dead code, state variables, context methods, and unused files are deleted, maintaining a functional, lightweight application that compiles cleanly.

## Tasks
- [ ] **Task 1: Remove Local LLM Worker and AI Suggestions**
  - Delete AI worker file (e.g. `src/workers/ai.worker.ts` or worker creation scripts).
  - Delete `src/features/planner/components/AISuggestionsModal.tsx`.
  - Remove LLM worker states, refs, initialization logic (`initLocalLLMWorker`, `aiWorkerRef`, `localLLMState`) from `BujoContext.tsx` and component tabs.
  - Remove "Alívio IA" and decomp button from `BulletItem.tsx` and others.
- [ ] **Task 2: Remove Brain Dump Feature**
  - Remove `BrainDumpTab.tsx` or related components.
  - Remove brain dump state variables, types, and references from the sidebar menu, active tabs list, and `BujoContext.tsx`.
- [ ] **Task 3: Remove ADHD Copilot & Real-Time Suggestions**
  - Remove `renderRealTimeSuggestions` from `RapidLog.tsx` and `BujoContext.tsx`.
  - Remove `getRealTimeSuggestions` or Copilot helpers in `src/utils/`.
- [ ] **Task 4: Remove AI in Habit Tracker**
  - Remove AI button / sync capabilities inside `HabitTracker.tsx` and `useHabits.ts`.
- [ ] **Task 5: Remove Focus Guide (Guia de Foco) & Someday/Maybe**
  - Remove `SomedayMaybeTab.tsx` file.
  - Remove Someday/Maybe sidebar tabs, active tabs, state array (`somedayItems`), handlers (`handleAddSomedayItem`, etc.), and storage keys.
  - Remove Focus Guide ("Guia de Foco" card/button next to daily bujo) and focus guidance modals.
- [ ] **Task 6: Clean Up Context & Validate Build**
  - Remove all dead exports, unused imports, types, and settings variables.
  - Ensure the project builds successfully.

## Verification Criteria
1. The sidebar/tabs menu has no references or links to "Brain Dump", "Algum Dia/Talvez", "Guia de Foco", or "IA".
2. No AI optimization, decomp, or suggestion options exist inside task items, daily logs, or rapid capture modal.
3. Rapid capture modal doesn't display TDAH Copilot suggestions.
4. No unused files (such as `AISuggestionsModal.tsx` or `SomedayMaybeTab.tsx`) remain in the repository.
5. The project compiles cleanly and build succeeds.
