---
phase: 22
plan: 22.1
type: auto
autonomous: true
depends_on: ["21.1"]
---

# Plan 22.1: Ordenar as tarefas por padrão (sem horário/data primeiro)

## Objective
Order task list views by default such that tasks without a specified execution time or date are listed first, followed by tasks with a scheduled date and time sorted chronologically.

## Tasks
- [ ] **Task 1: Add compareBujoItems sorting function to plannerUtils.ts**
  - Implement a sorting comparator `compareBujoItems` that puts items without a date first, then items with a date but no time, then items with a date and time sorted chronologically by date and then time. Fallback to `createdAt` (newest first).
- [ ] **Task 2: Use compareBujoItems in DailyLogTab.tsx**
  - Import `compareBujoItems` and sort `sortedDateItems` using it.
- [ ] **Task 3: Use compareBujoItems in DayTasksModal.tsx**
  - Import `compareBujoItems` and sort `dateItems` when filtering matching items.
- [ ] **Task 4: Use compareBujoItems in MonthlyLogTab.tsx**
  - Import `compareBujoItems` and sort `activeDateItems` when filtering active day items.
- [ ] **Task 5: Verify task build compiles successfully**
  - Compile the application and ensure no type or compilation errors exist.

## Verification Criteria
1. Tasks without time are grouped at the top of the daily log.
2. Tasks with time (e.g. `09:30`, `14:00`) are listed in chronological order below the no-time tasks.
3. Drag-and-drop sort styling works, and default sorting is applied correctly in Daily Log, Day Tasks Modal, and Monthly Log side panel.
4. The project builds cleanly using `npm run build`.
