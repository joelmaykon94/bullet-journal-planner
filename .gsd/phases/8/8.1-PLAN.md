---
phase: 8
plan: 1
wave: 1
---

# Plan 8.1: Daily Log Search, Sorting, Contexts, Icon Filters & TDAH Copilot Adjustments

## Objective
Enhance the Daily Log feature set by providing text search, sorting options (recent/oldest, energy, complexity, execution time), execution time tracking for tasks and micro-tasks, compact TDAH Copilot layout, `#` symbol delegation, updated contexts, and icon tooltips, searches, and category filters.

## Context
- src/types/index.ts
- src/hooks/useBujoItems.ts
- src/context/BujoContext.tsx
- src/features/planner/components/DailyLogTab.tsx
- src/features/planner/components/BulletItem.tsx

## Tasks

<task type="auto">
  <name>Update Types and Core Hooks for Energy, Complexity, and Execution Time</name>
  <files>
    <file>src/types/index.ts</file>
    <file>src/hooks/useBujoItems.ts</file>
    <file>src/context/BujoContext.tsx</file>
  </files>
  <action>
    - Add `energy`, `complexity`, and `executionTime` fields to `BujoItem` and `Subtask` types.
    - Update `handleSaveStandardInput` and `handleSaveEditItem` to support saving these properties and parsing `#` delegation names.
    - Update `addSubtask` in hook to support `executionTime` for micro-tasks.
    - Update `BujoContextType` interface and its wrapper `handleSaveEditItemForm` in BujoContext.
  </action>
  <verify>Verify types and hook exports compile cleanly.</verify>
  <done>Core data hooks and type definitions updated.</done>
</task>

<task type="auto">
  <name>Optimize TDAH Copilot and Subtasks Scroll Behavior</name>
  <files>
    <file>src/context/BujoContext.tsx</file>
    <file>src/features/planner/components/BulletItem.tsx</file>
  </files>
  <action>
    - Make `renderRealTimeSuggestions` in BujoContext extremely compact, inline suggestions as flex badges, and remove vertical scroll.
    - Restrict the subtasks container in `BulletItem` to `max-h-48 overflow-y-auto` so the parent task list stays clean.
  </action>
  <verify>Run dev compilation and check styles.</verify>
  <done>TDAH Copilot and subtasks scroll optimizations applied.</done>
</task>

<task type="auto">
  <name>Implement Search, Sorting, and Contexts in Daily Log</name>
  <files>
    <file>src/features/planner/components/DailyLogTab.tsx</file>
    <file>src/features/planner/components/BulletItem.tsx</file>
  </files>
  <action>
    - Replace `@celular` and `@trabalho` contexts with `@online`, `@trabalhando`, `@mestrado`, and `@programando`.
    - Implement a text search input in Daily Log to filter items.
    - Implement a sorting select to sort tasks by recent/oldest, energy, complexity, and execution time.
  </action>
  <verify>Verify filtering and sorting logic operates on the task list array.</verify>
  <done>Daily Log text search, sorting, and updated contexts implemented.</done>
</task>

<task type="auto">
  <name>Implement Tooltips, Searches, and Filters for Icons</name>
  <files>
    <file>src/features/planner/components/DailyLogTab.tsx</file>
    <file>src/features/planner/components/BulletItem.tsx</file>
  </files>
  <action>
    - Create a shared `BUJO_ICONS` configuration with tooltips and search terms.
    - Add an input inside the icon selector popup to search/filter icons.
    - Add hover tooltips to all icons.
    - Add an icon category filter row to filter Daily Log items by icon classes (dinheiro, familia, saude, arte, ideia).
  </action>
  <verify>Run npm run build to ensure complete compilation.</verify>
  <done>Icon tooltips, searches, and category filters implemented.</done>
</task>

## Success Criteria
- [ ] Daily Log items can be searched by text.
- [ ] Tasks can be sorted by energy, complexity, execution time, and date.
- [ ] Execution time can be specified for both tasks and subtasks.
- [ ] Subtask list scrolls within the task card, and TDAH Copilot is highly compact with no scroll.
- [ ] `#` prefix extracts delegation target.
- [ ] Context list updated to online, trabalhando, mestrado, programando.
- [ ] Icons have tooltips, search filters, and the daily log has category filters.
