---
phase: 9
plan: 2
wave: 1
depends_on: [9.1]
---

# Plan 9.2: Index Tab & Onboarding Improvements

## Objective
Improve the Index Tab layout and fix onboarding scrolling bugs:
1. Remove the duplicate Level & XP Progress Bar from the Index tab, keeping only the global one in the sidebar.
2. Enhance the Neuro-Adaptive Foco Central with more detailed statistics based on task states, active contexts, icon categories (themes), and delegated tasks.
3. Fix the onboarding layout bug where `scrollIntoView` shifts the locked viewport.

## Context
- src/features/planner/components/IndexTab.tsx
- src/components/common/TutorialOverlay.tsx

## Tasks

<task type="auto">
  <name>Remove Duplicate XP Bar from Index Tab</name>
  <files>
    <file>src/features/planner/components/IndexTab.tsx</file>
  </files>
  <action>
    - Delete the Lvl/XP progress bar div from the welcome section of `IndexTab.tsx`.
  </action>
  <verify>Verify that the XP bar is no longer displayed on the Index page and only appears in the sidebar.</verify>
  <done>Duplicate progress bar removed from the IndexTab welcome banner.</done>
</task>

<task type="auto">
  <name>Enhance Index Tab Neuro-Adaptive Central with Task Metrics</name>
  <files>
    <file>src/features/planner/components/IndexTab.tsx</file>
  </files>
  <action>
    - Import `BUJO_ICONS` from `./DailyLogTab`.
    - Compute counts for: general task statuses (total, pending, completed), active contexts (with corresponding icons), theme/category icons (Finanças, Família, Saúde, Arte, Ideia), and delegated tasks.
    - Render a clean, high-density grid showing these statistics at the bottom of the welcome banner.
  </action>
  <verify>Verify the computed counts update correctly based on existing task data.</verify>
  <done>Task status, context, category, and delegation metrics added to the index page central header.</done>
</task>

<task type="auto">
  <name>Fix Onboarding scrollIntoView Responsiveness Bug</name>
  <files>
    <file>src/components/common/TutorialOverlay.tsx</file>
  </files>
  <action>
    - Wrap the `scrollIntoView` call inside the spotlight effect with a check `element.closest('#bujo-export-area')`.
    - Only scroll into view if the highlighted element is inside the main scrollable workspace, avoiding shifts in the fixed sidebar or header.
  </action>
  <verify>Run the onboarding tour and check that the viewport is not shifted when highlighting elements in the sidebar or header.</verify>
  <done>Tutorial overlay viewport scroll shifting issue fixed.</done>
</task>

## Success Criteria
- [ ] No duplicate XP bar is rendered in the Index Tab welcome banner.
- [ ] Neuro-Adaptive Central displays counts for active contexts, delegated tasks, active categories, and task completion rates.
- [ ] The onboarding tour highlights sidebar/header items without shifting or breaking layout responsiveness.
